import SubItem from './SubItem'

const SidebarSubscriptions = () => {
	return (
		<div>
			<ul>
				{/* {menu.map(menuItem => (
					<SubItem
						key={menuItem.label}
						item={menuItem}
					/>
				))} */}
			</ul>
		</div>
	)
}

export default SidebarSubscriptions
